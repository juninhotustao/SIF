GO

CREATE TABLE MESA(
	MES_ID INT PRIMARY KEY IDENTITY(1,1) NOT NULL,
	MES_DATAHORA DATETIME NOT NULL,
	MES_DATAHORA_FECHAMENTO DATETIME,
	MES_SITUACAO CHAR(1) NOT NULL,
	MES_TOTAL NUMERIC(15, 2) NOT NULL DEFAULT 0	
)

GO

CREATE TABLE ITEM_MESA(
	ITS_ID INT PRIMARY KEY IDENTITY(1,1) NOT NULL,
	MES_ID INT NOT NULL,
	PRO_ID INT NOT NULL,
	ITS_REFER VARCHAR(20) NOT NULL,
	ITS_DATAHORA DATETIME NOT NULL,
	ITS_DESCONTO NUMERIC(15, 2) NOT NULL DEFAULT 0,
	ITS_VLR_UNIT NUMERIC(15, 2) NOT NULL DEFAULT 0,
	ITS_VLR_TOTAL NUMERIC(15, 2) NOT NULL DEFAULT 0,
	ITS_SITUACAO CHAR(1),
	CONSTRAINT FK_ITEM_MESA_MES_ID FOREIGN KEY (MES_ID) REFERENCES MESA (MES_ID),
	CONSTRAINT FK_ITEM_MESA_PRO_ID FOREIGN KEY (PRO_ID) REFERENCES PRODUTOS (PRO_ID)	
)

GO